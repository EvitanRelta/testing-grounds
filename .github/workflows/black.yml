name: Check if .py files have been formatted by Black

on: [push, pull_request]

jobs:
  check-black-formatted:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@stable
